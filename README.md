Проект создан в качестве финального проекта, выполняет роль веб-приложения планировщика задач.

Задания повышенной трудности на данный момент не выполнялись.

Пароль задаётся в пакете signin.

Стандартные параметры для тестов (settings.go):

var Port = 7540
var DBFile = "../scheduler.db"
var FullNextDate = false
var Search = false
var Token = ``

Сборка и запуск через докер происходит с помощью "docker-compose up --build"
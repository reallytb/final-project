services:
  web:
    build: .
    ports: 
    - "7540:7540"
    volumes: 
    - .:/usr/src/app
    command: go run main.go
    depends_on:
    - db
  db:
   image: nouchka/sqlite3:latest
   ports: 
     - "5432:5432"
   restart: unless-stopped
   volumes:
     - ./scheduler.db:/scheduler.db

